# Optional publisher only. Export these in your shell; no automatic dotenv loading.
THINGSBOARD_HOST=
THINGSBOARD_TOKEN=
THINGSBOARD_PORT=8883
# Firmware uses an ignored secrets.h; see firmware/README.md.
